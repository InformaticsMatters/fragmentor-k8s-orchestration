---

# The Kubernetes config file.
# Rendered as 'config' in '$HOME/.kube'

kind: ConfigMap
apiVersion: v1
metadata:
  name: kubeconfig
  namespace: {{ fp_namespace }}
data:
  config: "{{ kubeconfig_fact|replace('\n', '\\n') }}"
